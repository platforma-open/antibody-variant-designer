---
"@platforma-open/milaboratories.antibody-variant-designer.block": minor
---

Carry each step artifact in one keyed file instead of a directory of per-antibody files, so a dataset of several thousand antibodies runs instead of failing before any analysis starts. Show the parent sequence stacked beside the designed variant in the comparison view.
