{
  "name": "@platforma-open/milaboratories.antibody-variant-designer.software-developability.workflow",
  "version": "1.0.0",
  "private": true,
  "description": "Exec recipes for the developability entrypoints",
  "type": "module",
  "scripts": {
    "build": "shx rm -rf dist && pl-tengo build",
    "check": "pl-tengo check",
    "test": "pl-tengo test",
    "format": "/usr/bin/env emacs --script ./format.el || echo 'No emacs.'"
  },
  "dependencies": {
    "@platforma-open/milaboratories.antibody-variant-designer.software-antifold": "workspace:*",
    "@platforma-open/milaboratories.antibody-variant-designer.software-developability": "workspace:*",
    "@platforma-sdk/workflow-tengo": "catalog:"
  },
  "devDependencies": {
    "@platforma-sdk/tengo-builder": "catalog:",
    "@platforma-sdk/test": "catalog:",
    "shx": "catalog:"
  }
}
